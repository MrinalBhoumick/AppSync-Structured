#if( $context.result && $context.result.error )
    $util.error($context.result.error.message)
#else
    #if( $context.info.fieldName == "getTaskRevenueIssueList" )
        #set($result = $util.parseJson($context.result))
        "'{availableTaskIds: $util.toJson($result.availableTaskIds)}'"
    #else
        $util.toJson($context.result)
    #end
#end